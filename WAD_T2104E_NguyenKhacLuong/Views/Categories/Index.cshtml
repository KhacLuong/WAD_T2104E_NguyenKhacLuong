@model IEnumerable<WAD_T2104E_NguyenKhacLuong.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<p style="color: #ff0000;">@TempData["message"]</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CotegoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CotegoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CotegoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CotegoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CotegoryId })
        </td>
    </tr>
}

</table>
